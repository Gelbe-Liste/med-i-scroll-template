# QA 08.09.2026 – med.i.scroll Mastertemplate v5

## Packaging-Status

- Basis: `2026-09-02_med-i-scroll-master-template_v4`
- Zielversion: v5
- Piano-Konfiguration im Mastertemplate: **deaktiviert** (`VITE_PIANO_ENABLED=false`)
- Site-ID/Collection Domain als aktuelle Vorgabe vorbereitet: `640794` / `https://rwwnhth.pa-cd.com`

## Durchgeführte Prüfungen

- JavaScript-/JSX-Syntax mit TypeScript-Parser geprüft: keine Parse-Fehler.
- `package.json` und `manifest.webmanifest` syntaktisch geprüft.
- Alle in `src/project.js` referenzierten `/assets/...`-Dateien auf Existenz geprüft.
- Tracking-Review: bestehende Events `page.display`, `click.action`, `pop_in.display` priorisiert.
- Custom-Scrolltracking auf `page.scroll` + `scroll_rate` 25/50/75/100 konsolidiert.
- 100-%-Milestone wird nur am tatsächlichen Dokumentende ausgelöst; die frühere pauschale 98,5-%-Schwelle entfällt.
- Kapitel-Reach dedupliziert über `chapter.display` + `trackOnce()`.
- Grafik-, PDF-, CTA- und Navigationsinteraktionen auf `click.action` vereinheitlicht.
- Zoomtracking dedupliziert; nicht jede Wheel-/Pinch-Bewegung erzeugt ein Event.
- PDF-Download wird erst nach erfolgreicher Generierung als `Download PDF` erfasst.
- Ziel-URLs werden als bereinigter `destination_path` ohne Query-/Hash-Parameter übertragen.
- `__MALARIA_TRACKING__` vollständig durch generische `__MEDI_SCROLL_TRACKING__`-Queue ersetzt.
- `user_id`, `occupation`, `speciality_function` und `exercise_mode` werden nicht erzeugt.

## Noch vor Produktivsetzung erforderlich

1. Neue Properties im Piano Data Model anlegen/validieren.
2. Custom Events `chapter.display`, `page.scroll` und – falls genutzt – Videoevents als **On Site** anlegen.
3. Entscheidung treffen: Custom-Videoevents vs. native Piano A/V-Insights-Events.
4. Essential-Einstufung der neuen Elemente intern mit Analytics/Datenschutz freigeben.
5. `VITE_PIANO_ENABLED=true` ausschließlich im freigegebenen Produktivprojekt setzen.
6. Browser-QA mit Piano Tag Inspector/Stream Inspector durchführen.
7. Finalen Vercel-Build nach Installation der npm-Abhängigkeiten ausführen.

## Build-Hinweis dieser QA-Umgebung

Ein vollständiger `npm ci`/`npm run build` konnte in der aktuellen isolierten Ausführungsumgebung nicht abgeschlossen werden, weil die npm-Registry temporär nicht auflösbar war (`EAI_AGAIN`). Die Quellcode-Syntax-, JSON- und Asset-Prüfungen sind erfolgreich; der produktive Build bleibt als Deployment-Gate bestehen.
